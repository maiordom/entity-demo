import React from 'react';

import TestButton from 'src/components/TestButton/TestButton';

export default class TestPage extends React.Component<any, any> {
    render() {
        return (
            <div>
                <TestButton>UI Button</TestButton>
            </div>
        );
    }
}
